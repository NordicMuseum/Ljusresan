# Nordiskt ljus
RFID proxy/command unit

## Prerequisites
- docker (for mac)
- docker-compose

## Development
- `$ docker-compose up`

Note: Run `yarn install` in every directory containing `yarn.lock`.
